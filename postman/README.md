# Postman (Guideline 3.6)

Export the tested collection here as `AirQuality.postman_collection.json`,
demonstrating GET/POST to the three DRF endpoints with sample responses:

- `GET  /api/readings/`
- `GET  /api/predictions/`
- `POST /api/predict/`
